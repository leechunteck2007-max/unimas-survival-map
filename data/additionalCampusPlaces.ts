import type { CampusPlaceInput } from "@/types/campus-place";

// Add source-backed places here. Empty categories are intentionally represented
// in the category configuration instead of being filled with development data.
export const additionalCampusPlaces: CampusPlaceInput[] = [];
